// Identifiquen que variables deben ser constantes (const) y que otras variables
// deben ser (let)


let nombre = 'Juan';  // debe ser  let
let instituto = 'IDAT'; //  debe ser const
let edad = 19;  // debe ser let
let nombreDeMascota = 'Scooby'; // debe ser let
let ciudad = 'Lima'; // debe ser let

console.log(
	'Hola Mundo soy ' +
		nombre +
		' y estoy estudiando en ' +
		instituto +
		' y tengo ' +
		edad +
		' años'
);
